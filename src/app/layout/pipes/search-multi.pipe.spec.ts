import { SearchMultiPipe } from './search-multi.pipe';

describe('SearchMultiPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchMultiPipe();
    expect(pipe).toBeTruthy();
  });
});
